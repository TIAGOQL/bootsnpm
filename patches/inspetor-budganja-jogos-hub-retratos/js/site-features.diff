diff --git a/js/site-features.js b/js/site-features.js
index 79dbb2e..c418368 100644
--- a/js/site-features.js
+++ b/js/site-features.js
@@ -733,8 +733,8 @@
     btn.dataset.ytBound = '1';
     btn.addEventListener('click', function () {
       var id = btn.getAttribute('data-youtube-id') || '';
-      // Home / sala de vídeo: play in-page com som (sem mute) + legendas CC.
-      if (btn.closest('.home-vida-embed')) {
+      // Home / sala de vídeo / hub Jogos: play in-page com som (sem mute) + legendas CC.
+      if (btn.closest('.home-vida-embed, .jogos-hub-embed')) {
         loadYoutubeFacade(btn, true);
         return;
       }
