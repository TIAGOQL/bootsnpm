'use strict';

// Versão usada para cache-busting dos assets estáticos (?v=...).
// Incrementar quando for preciso forçar navegadores a descartarem CSS/JS antigos.
const ASSET_VERSION = '314';

module.exports = { ASSET_VERSION };
